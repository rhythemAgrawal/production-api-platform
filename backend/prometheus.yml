global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["localhost:9464"]
        labels:
          # An external label so multi-env dashboards stay sane later.
          cluster: local